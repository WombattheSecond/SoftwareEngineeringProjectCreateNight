name: Notify Portfolio

on:
  push:
    branches:
      - main

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: Notify Personal Portfolio
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_PORTFOLIO }}
        run: |
          curl -L \
            -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            https://api.github.com/repos/WombattheSecond/Personal_Portfolio/dispatches \
            -d '{
              "event_type": "sync-project",
              "client_payload": {
                "project": "schedule"
              }
            }'
